# sort by target_name

import pandas as pd
>> Use this to make list
match kmer len 11

make a list: 
	where i have (target name, target start, query start, kmer)



		 
for ident, sequence in fasta_fixed.FASTAReader(target):
    sequence = sequence.upper()
    for i in range(0, len(sequence)-k_len):
        kmer = sequence[i : i+k_len]
        target_start = i
        if kmer in query_start:
            
            
            
            for sample in df_meta[ "sample" ]:
                filename = ctab_dir + "/" + sample + "/t_data.ctab"
                df = pd.read_table( filename )

            
##for target_start in list
for list(2) in list:
	i = list(2)
	j = i + 1
	if j = 



	else
		print kmer
	

if kmer0 in kmer_dict:
	##extend by 1
	target_start in xxx
		 t0 = target_start [i]
		 t1 = 1 + t
		 if 